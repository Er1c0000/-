# 阶段3 未决问题

## 当前仍需用户确认的问题

- 无。

## 当前仍缺少证据的问题

1. `正交化`、`条件化再估计`、`相关性约束联合再估计` 三类局部解耦路线中，哪一条最适合作为正式实现主线，当前仍缺少对比证据。
2. `高相关风险触发阈值` 的具体定义，当前仍缺少实验支撑。
3. `一致性检查` 应优先依赖哪一组诊断指标，当前仍缺少阶段4验证。

## 当前需留待阶段4验证的问题

1. 在不同 `PJ` 频率、`ISI` 强度、`RJ` 水平和样本量下，哪些区域最需要触发局部解耦。
2. 局部解耦后是否会出现 `ISI` 改善但 `PJ` 失真加剧的副作用。
3. 当前框架在多 `PJ` 共存场景中是否仍保持前端主线的稳健性。
4. 阶段3新增模块的评价接口，应优先输出哪些指标供阶段4统一比较。

## 当前保留但不在阶段3解决的问题

1. 若后续需要强化 `抖动建模` 主线，应如何通过章节接口吸收，而不改写当前分解增强框架。
2. `有限样本偏差修正` 是否作为后续辅助模块吸收，当前不在阶段3决定。

## 下一步建议

- 当前阶段3可视为完成。
- 下一步建议进入 `阶段4：实验设计`，围绕上述未决问题组织变量、指标、基线和验证场景。
