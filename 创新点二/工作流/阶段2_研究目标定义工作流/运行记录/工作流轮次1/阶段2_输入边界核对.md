# 阶段2 输入边界核对

## 本轮目标

- 要定义的第二创新点问题：
  - `低频 PJ-ISI 共线性条件下的弱分量稳定分解问题`
- 本轮要解决的唯一局部问题：
  - 在不重回候选筛选、也不提前进入方法设计的前提下，锁定阶段2的正式输入口径。

## 输入材料

- 上游正式输入：
  - `创新点二\正式文档\02_创新点方案\创新点定义.md`
- 开题主线：
  - `面向高速示波器的复杂抖动分解与建模研究`
- 已完成小论文已覆盖内容：
  - 基于冗余频率字典、FastRVM 与 ARD 的 SBL 多分量抖动分解框架；
  - 已完成 `PJ` 频率偏移修正和多 `PJ` 场景的联合分解验证。
- 当前明确限制：
  - 不读取受限目录；
  - 不把未完成工作写成既成结果；
  - 不默认转向非平稳抖动；
  - 当前验证边界以仿真和公开可复现实验为主。
- 必要过程材料入口：
  - `创新点二\工作流\阶段1_创新点方向确认工作流\运行记录\工作流轮次2\阶段一_总约束文档.md`
  - `创新点二\工作流\阶段1_创新点方向确认工作流\运行记录\工作流轮次2\阶段五_最终方案文档.md`

## 边界核对结果

- 当前正式方向输入已明确为：
  - `面向低频 PJ-ISI 共线性的结构化解耦分解方法`
- 当前阶段2不再解决的问题：
  - 不重新比较候选创新点；
  - 不把方向扩写为完整方法方案；
  - 不把创新点直接拉宽为完整 `抖动建模` 体系。
- 当前阶段2必须解决的问题：
  - 把上述方向收束成稳定的问题定义、研究目标、研究边界和完成标准；
  - 写清它与第一创新点的承接关系和差异；
  - 用真实文献说明该问题仍值得做。

## 本轮关键判断

1. 第二创新点的正式输入口径应以 `创新点定义.md` 为准，而不是继续依赖历史草稿中的发散表述。
2. 第一创新点已占位的是 `选频与联合分解`，阶段2需要占位的是 `高相关子空间中的弱分量稳定恢复`。
3. 虽然大论文题目包含 `抖动建模`，但本阶段更稳妥的任务仍是先把第二创新点在 `抖动分解增强` 侧的问题定义压稳，再在后续阶段设计与建模主线的接口。

## 尚未解决的问题

- 具体采用何种结构化解耦实现，不属于本轮边界核对内容；
- 若后续导师要求显著强化 `抖动建模` 章节比重，需要在阶段3单独处理章节接口。

## 下一步建议动作

- 进入 `阶段2_研究目标定义草案.md` 的正式写作与压缩；
- 同步补齐文献检索记录，保证“为什么值得做”的判断可回溯。
